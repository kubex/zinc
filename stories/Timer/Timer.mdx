import {Canvas, Controls, Meta} from '@storybook/blocks';

import * as TimerStories from './Timer.stories';

<Meta of={TimerStories}/>

# Definition

<Canvas of={TimerStories.Default}/>

## Usage

The component comes in different variants such as `error`, `success`

## Inputs

<Controls/>
